# generate ssh key #

ssh -keygen -t rsa -c burns@blue

# the public key generated is assigned to a username via an admin who has permission #

# logging onto the server #

ssh asaba@147.188.173.201 

# logging out of the server can be done by pressing Ctrl + d #

# create conda environment with python version 3.10 #

conda create -–strep_project python=3.10 mamba-y

# activate that conda environment #

conda activate strep_project 

# the environment should then change from (base) to (strep_project) #

# to deactivate that environment  #

conda deactivate 
